To build both parts of the assignment run buildall.sh from this directory.

The two parts of the assignment can be run from this directory by executing ./run_part_1.sh and ./run_part_2.sh respectively.
